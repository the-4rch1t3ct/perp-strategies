# Copy to .env and fill in for live dashboard data (positions, equity, win rate from exchanges).
# Without these, the dashboard uses JSON files only (can be stale/wrong).

# Hyperliquid: account address (no private key needed for positions + user fills)
HL_ACCOUNT_ADDRESS=0xYourHyperliquidAddress

# Optional: HL_BASE_URL, HL_DEX if different from default

# Aster: for live positions + equity (EIP-712 signed API)
# ASTER_API_KEY=your_key
# ASTER_API_SECRET=your_secret
# ASTER_USER_ADDRESS=0x...
# ASTER_API_SIGNER=0x...
